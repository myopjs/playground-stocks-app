import { ReactNode } from "react";
interface MyopFallbackProps {
    children?: ReactNode;
}
export declare const MyopFallback: ({ children }: MyopFallbackProps) => import("react").JSX.Element;
export {};
